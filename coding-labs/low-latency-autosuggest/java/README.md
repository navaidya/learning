# Java runner

From this directory run `javac -d out src/main/java/AutosuggestIndex.java src/test/java/AutosuggestIndexTest.java && java -ea -cp out AutosuggestIndexTest` with JDK 17 or newer. Success is a silent exit code 0. Cleanup: `rm -rf out`.
