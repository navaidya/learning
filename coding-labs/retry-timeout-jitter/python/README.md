# Python

From this directory, run `python3 -m unittest discover -s tests -v`. Expected result: all retry, permanent-failure, and budget tests pass. Cleanup: no virtual environment or generated files are required; remove `__pycache__` only if your interpreter creates it.
