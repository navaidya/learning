# Python runner

From this directory run `python3 -m unittest discover -s tests -v` with Python 3.10 or newer. Success reports three passing tests. Cleanup: remove any local `__pycache__` directories if your runtime creates them; no virtual environment is required.
