# Java runner

From this directory run `javac -d out src/main/java/MergeKSortedLists.java src/test/java/MergeKSortedListsTest.java && java -ea -cp out MergeKSortedListsTest` with JDK 17 or newer. Success is a silent exit code 0. Cleanup: `rm -rf out`.
