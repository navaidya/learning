# Python

From this directory, run `python3 -m unittest discover -s tests -v`. Expected result: all tests pass, including atomic burst admission. Cleanup: no virtual environment or generated files are required; remove `__pycache__` only if your interpreter creates it.
