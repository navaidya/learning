# Java runner

From this directory run `javac -d out src/main/java/FirstOccurrenceSearch.java src/test/java/FirstOccurrenceSearchTest.java && java -ea -cp out FirstOccurrenceSearchTest` with JDK 17 or newer. Success is a silent exit code 0. Cleanup: `rm -rf out`.
